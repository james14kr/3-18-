<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 해당 파일에 모든 쿼리문을 작성 -->
<!-- namespace에는 xml파일과 연결할 Mapper interface 파일명을 패키지를 포함하여 적어주세요 -->
<mapper namespace="com.green.VehicleSalesInformationSystem.car.mapper.CarMapper">

    <resultMap id="carInfo" type="com.green.VehicleSalesInformationSystem.car.dto.CarDTO">
        <id column="CAR_NUM" property="carNum"/>
        <result column="MODEL_NUM" property="modelNum"/>
        <result column="MODEL_NAME" property="modelName"/>
        <result column="PRICE" property="price"/>
        <result column="COMPANY" property="company"/>
    </resultMap>

    <insert id="insertCarInfo">
        INSERT INTO CAR_INFO(
            MODEL_NUM
            ,MODEL_NAME
            ,PRICE
            ,COMPANY
        )VALUES(
            #{modelNum}
            ,#{modelName}
            ,#{price}
            ,#{company}
        )
    </insert>

    <select id="selectCarInfo" resultMap="carInfo">
        SELECT
            MODEL_NUM
            ,MODEL_NAME
            ,COMPANY
        FROM CAR_INFO
        ORDER BY MODEL_NUM DESC;
    </select>
	
</mapper>































